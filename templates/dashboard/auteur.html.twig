{% extends 'dashboard.html.twig' %}

{% block body %}

	<div class="container">

		<div class="page-title">
			<h3>Auteurs
				<a href="{{path('dashboard_auteur_add')}}" class="btn btn-sm btn-outline-primary float-end">
					<i class="fas fa-plus"></i>
					Ajouter Auteur</a>
			</h3>
		</div>
		<div class="box box-primary">
			<div class="box-body">
				<div id="dataTables-example_wrapper" class="dataTables_wrapper dt-bootstrap5 no-footer">
					<div class="row">
						<div class="col-sm-12">
							<table width="100%" class="table table-hover dataTable no-footer dtr-inline" id="dataTables-example" role="grid" aria-describedby="dataTables-example_info" style="width: 100%;">
								<thead>
									<tr role="row">
										<th>Nom 	&amp; Prenom</th>
										<th>Sexe</th>
										<th>Date de naissance</th>
										<th>Nationalite</th>
										<th></th>

									</tr>
								</thead>
								<tbody>

									{% for auteur in auteurList %}
										<tr class="odd">
											<td>{{auteur.nom_prenom}}</td>
											<td>{{auteur.sexe == 'M' ? 'Mâle' : 'Femelle'}}</td>
											<td>{{auteur.date_de_naissance|date("m/d/Y")}}</td>
											<td>{{auteur.nationalite}}</td>
											<td class="text-end">
												<a href="{{path('dashboard_auteur_edit',{id:auteur.id})}}" class="btn btn-outline-info btn-rounded">

													<i class="fas fa-pen"></i>
												</a>

											</td>
										</tr>
									{% endfor %}

								</tbody>
							</tbody>
						</table>
					</div>
				</div>

			</div>
		</div>
	</div>
</div>{% endblock %}
